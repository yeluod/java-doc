# Profile: compatible

适用场景：
- 存量老项目
- 历史代码注释风格不统一
- 先做低风险增量治理

## 规则覆盖

- 保留并修复已有标签，不强制新增类型级 `@author` / `@version` / `@since`。
- 如果已存在 `@since`，必须修复为版本语义。
- `void` 方法禁止 `@return`。
- 非 `void` 方法补齐 `@return`。
- `@param` 与签名保持一致，优先修复错误参数名与顺序。

## 推荐启用时机

- 首轮注释清洗，重点降低噪音改动。
- 需要与既有代码评审习惯兼容。
